import type { SalesTab } from './salesTypes'

interface SalesHeaderProps { tab: SalesTab; onTab: (tab: SalesTab) => void; onExport: () => void; onNew: () => void }
export function SalesHeader({ tab, onTab, onExport, onNew }: SalesHeaderProps) { return <div className="section-header"><div><h2 className="section-title">Embudo, pedidos y facturación</h2><span style={{ fontSize: '0.8rem', color: 'var(--text-dim)' }}>Visual comercial conectada a pedidos, canales y catálogo del backend</span></div><div className="period-pills" role="tablist" aria-label="Secciones de ventas"><button type="button" className={`pill ${tab === 'embudo' ? 'active' : ''}`} onClick={() => onTab('embudo')}>Embudo</button><button type="button" className={`pill ${tab === 'pedidos' ? 'active' : ''}`} onClick={() => onTab('pedidos')}>Pedidos</button><button type="button" className={`pill ${tab === 'ventas' ? 'active' : ''}`} onClick={() => onTab('ventas')}>Ventas</button></div>{tab !== 'embudo' && <div style={{ display: 'flex', gap: '8px' }}><button type="button" className="btn-outline" onClick={onExport}><i className="ti ti-download" /> Exportar CSV</button><button type="button" className="btn-primary" onClick={onNew}><i className="ti ti-plus" /> {tab === 'pedidos' ? 'Nuevo Pedido' : 'Nueva Venta'}</button></div>}</div> }
